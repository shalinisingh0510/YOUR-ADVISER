NODE_ENV=production
PORT=5000
JWT_SECRET=replace-with-strong-secret
JWT_EXPIRES_IN=7d

# Preferred on Render
DATABASE_URL=postgres://user:password@host:5432/dbname
DB_SSL=true

# Optional fallback if DATABASE_URL is not used
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_advisor
DB_USER=postgres
DB_PASSWORD=postgres
